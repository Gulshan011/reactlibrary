




<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        font-family: Poppins, sans-serif;
        text-align: center; /* Center all text */
      }
      .container {
        max-width: 500px;
        margin: 10cm; /* Center the container */
        border: 2px solid #007BFF;
        border-radius: 10px;
        padding: 20px;
        box-sizing: border-box;
        box-shadow: #0069D9;
        border-radius: 25px;
        border: 2px solid #73AD21;
        padding: 20px;
        width: 200px;
        height: 150px;
        position: center;
      }
      h1 {
        color: #333;
        font-size: 28px;
        margin-bottom: 20px;
      }
      form {
        max-width: 600px;
        height: 500px;
        margin: 0 auto;
        background-color:  #4A4A74;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      label,
      input[type="password"] {
        display: block;
        width: 100%;
        margin-bottom: 10px;
        border: none;
        border-bottom: 1px solid #ccc;
        border-radius: 20px;
        padding: 10px;
        font-size: 20px;
        font-family: Poppins, sans-serif;;
        color: rgb(12, 11, 11);
        text-align: center; /* Center the text in labels and input fields */
        margin-top: 30px; /* Add margin-top to labels */
      }
      input[type="submit"] {
        background-color: #525F7F;
        border: none;
        color: rgb(247, 244, 244);
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        border-radius: 5px;
        transition: all 0.3s ease-in-out;
        margin-top: 20px; /* Add some margin at the top of the submit button */
        margin-bottom: 0; /* Remove margin-bottom for the submit button */
      }
      input[type="submit"]:hover {
        background-color: #D69111;
      }
      .error {
        color: rgb(226, 172, 23);
        font-weight: bold;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <h1>Please verify your <%= email %></h1>
    <form action="" method="post" id="password-form">
      <label for="password">Password</label>
      <input type="password" name="password" id="password" />
      <label for="confirm-password">Confirm Password</label>
      <input type="password" name="confirm-password" id="confirm-password" />
      <input type="submit" value="Submit" />
    </form>
    
    <script>
      const form = document.querySelector("#password-form");
      const password = document.querySelector("#password");
      const confirmPassword = document.querySelector("#confirm-password");
      form.addEventListener("submit", function (event) {
        event.preventDefault(); // Prevent form from submitting initially
        if (password.value === "") {
          const errorEl = document.createElement("p");
          errorEl.classList.add("error");
          errorEl.innerText = "Please enter a password.";
          form.appendChild(errorEl);
        } else if (confirmPassword.value === "") {
          const errorEl = document.createElement("p");
          errorEl.classList.add("error");
          errorEl.innerText = "Please confirm your password.";
          form.appendChild(errorEl);
        } else if (password.value !== confirmPassword.value) {
          const errorEl = document.createElement("p");
          errorEl.classList.add("error");
          errorEl.innerText = "Passwords do not match. Please check your password.";
          form.appendChild(errorEl);
        } else {
          form.submit(); // Submit the form if validation passes
        }
      });
    </script>
  </body>
</html>












